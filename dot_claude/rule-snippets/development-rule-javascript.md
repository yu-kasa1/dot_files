# 開発ルール JavaScript

## 変数宣言
- `var`は使用禁止。`const`を優先し、再代入が必要な場合のみ`let`を使用する。

## 比較演算子
- `==`/`!=`ではなく`===`/`!==`を使用する。

## 非同期処理
- ネストの深いコールバックを避け、`async/await`を使用する。

## エラーハンドリング
- `async/await`使用時は`try-catch`で適切にエラーをハンドリングする。
